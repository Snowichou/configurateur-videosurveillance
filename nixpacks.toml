# Configuration Nixpacks pour Railway
# Le frontend est déjà buildé (dist/ committé)

[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "uvicorn backend.app:app --host 0.0.0.0 --port ${PORT:-8000}"
