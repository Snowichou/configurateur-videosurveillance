[phases.setup]
nixPkgs = ["nodejs-20_x", "python313"]

[phases.install]
cmds = [
    "cd frontend && npm ci",
    "pip install -r backend/requirements.txt"
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "uvicorn backend.app:app --host 0.0.0.0 --port $PORT"